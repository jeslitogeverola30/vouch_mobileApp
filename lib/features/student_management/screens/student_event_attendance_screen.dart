import 'package:flutter/material.dart';

class StudentEventAttendanceScreen extends StatelessWidget {
  const StudentEventAttendanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Student Event Attendance Screen')),
    );
  }
}

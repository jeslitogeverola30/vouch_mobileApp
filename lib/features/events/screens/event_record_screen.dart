import 'package:flutter/material.dart';

class EventRecordScreen extends StatelessWidget {
  const EventRecordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Event Record Screen')));
  }
}

import 'package:flutter/material.dart';

class CreateNewFeeScreen extends StatelessWidget {
  const CreateNewFeeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Create New Fee Screen')),
    );
  }
}
